#!/usr/bin/env node

const program = require('commander');
const app = require('../package.json');

program
  .version(app.version)
  .command('start', 'Start local development environment')
  .command('seed', 'Seed database with data')
  .command('migrate', 'Create/run database migrations')
  .parse(process.argv);
